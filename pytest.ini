[pytest]
testpaths = backend/tests
python_files = test_*.py
filterwarnings =
    # The default `jwt_secret` is 23 bytes, below RFC 7518's 32-byte minimum for
    # HS256. Real, and recorded in ETAPA16_REPORT.md — surfaced, not hidden.
    ignore:The HMAC key is .* bytes long:UserWarning
